system:
    default:
        fielddefinition_settings_templates:
            -
                template: "{{ bundle_basename }}Bundle::fielddefinition_settings.html.twig"
        fielddefinition_edit_templates:
            -
                template: "{{ bundle_basename }}Bundle::field_types.html.twig"
        field_templates:
            -
                template: "{{ bundle_basename }}Bundle::content_fields.html.twig"

